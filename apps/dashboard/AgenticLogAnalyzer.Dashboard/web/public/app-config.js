// Runtime configuration. The desktop app serves its own version of this file ({ apiBase: "" } = same origin);
// the web dashboard keeps the default and uses VITE_API_BASE_URL or http://localhost:5080.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {}
